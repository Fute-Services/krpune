// Preload runs in an isolated context before the renderer loads.
// The app needs no privileged bridge (fully offline, static data), so this is a
// deliberate no-op placeholder kept for security (contextIsolation stays on).
